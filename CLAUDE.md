# Шкаф книговыдачи (RPi 3)

> Платформа подтверждена Романом 2026-06-12: Raspberry Pi 3 (упоминания RPi 4 в ТЗ/replit.md — ошибочны).
> Прежний развёрнутый CLAUDE.md сохранён в `docs/archive/CLAUDE_v1_2026-04.md`; его аппаратные факты перенесены в `docs/HARDWARE.md`, состояние и противоречия — в `docs/STATE.md`.

## Архитектура

Шкаф автоматической книговыдачи: веб-киоск «взять/вернуть книгу», БД (книги/ячейки/пользователи),
RFID-идентификация (читательский билет NFC, карта ЕКП UHF, метки книг UHF), транспортный механизм
(CoreXY-каретка + лоток забирает полку с книгой из ячейки и везёт к окну выдачи), интеграция с ИРБИС.

Боевая цепочка на шкафу (systemd): `pigpiod → bookcabinet-calibration → bookcabinet-daemon + bookcabinet-ui (node dist/index.js, порт 5000) → chromium-kiosk`.

- **Боевой сервер — Node/Express** (`server/routes.ts` + WebSocket). Python вызывается субпроцессом: `python3 -m bookcabinet.bridge <cmd>` → `bookcabinet/business/*` (стейт-машины над БД) и `bookcabinet/workflows/*` (18/19-шаговые боевые сценарии) → скрипты `tools/*.py`.
- Параллельно существует **полный Python aiohttp-сервер** `bookcabinet/main.py` — не задеплоен. **Решение 2026-06-12: целевой бэкенд — Python aiohttp**; Express остаётся боевым до проверенной миграции (план в docs/STATE.md).
- Фронтенд: React в `client/`, сборка vite → `dist/public/`.
- БД: SQLite `bookcabinet/data/shelf_data.db` (`bookcabinet/database/`). Drizzle/PostgreSQL-схема в `shared/schema.ts` НЕ используется.
- Механика: канонический слой движения — `tools/corexy_motion_v2.py`; приложение ходит через `mechanics/algorithms.py` → `hardware/motors.py`. HOME = LEFT+BOTTOM, скорости хоминга 800/300 — живые истины, не менять.
- RFID: `bookcabinet/rfid/unified_card_reader.py` (NFC ACR1281U-C + UHF IQRFID-5102). RRU9816 (метки книг) — драйвер есть, в опрос не подключён; **нужен в проде, подключить** (Роман, 2026-06-12). По словам Романа физически стоят 2 одинаковых считывателя + 1 другой — сверить с конфигом при доступе к RPi.
- ⚠️ На RPi лежат рабочие скрипты управления механикой, которых нет (или которые новее) в репо — при доступе к шкафу найти и забрать в репозиторий, до этого считать репо-версии потенциально отстающими.
- ИРБИС: `bookcabinet/irbis/` (TCP 6666, офлайн-очередь, мок).

Карта документации: `docs/STATE.md` (что работает/не доделано), `docs/HARDWARE.md` (пины/порты/устройства),
`docs/SOURCES_OF_TRUTH.md` (источники истины по подсистемам), `docs/DEVLOG.md`, `docs/DECISIONS.md`.
Не каждый .md свежий; при конфликте — верить коду и свежим докам из этого списка.

## Механика — ЖЕЛЕЗНЫЕ ПРАВИЛА

Транспортный механизм: каретка забирает полку из ячейки и везёт к окну.

- НИКОГДА не запускать физическое движение без явного разрешения в сессии
- Перед любым движением — homing/проверка позиции
- Один тестовый прогон = одна операция, никаких циклов
- Неожиданное значение датчика/концевика = СТОП, не "пробовать ещё раз"
- Таймаут обязателен на каждое движение

Дополнительно (выводы живых сессий на шкафу, подробности в docs/HARDWARE.md):

- HOME = LEFT + BOTTOM. Не возвращать RIGHT_BOTTOM из старых скриптов.
- Скорости хоминга FAST=800 / SLOW=300; 1500/400 рвали ремень, >3000 — клин. Не повышать.
- Перед движением лотка пины 25 и 26 — в LOW.
- Glitch-фильтры на концевиках не убирать; GPIO 20 шумит, нужен фильтр 5000 мкс.
- Не «нормализовать» полярность/подтяжку датчиков по старым докам — проверять текущий скрипт.
- Отладочные/калибровочные скрипты не удалять, пока их знания не перенесены в доки.

## Режимы

MOCK_MODE=true — обязателен везде, кроме согласованных тестов на RPi.

На этом ноутбуке железа нет — только мок-режим.

Примечание по коду:

- `config.py:70` принимает только `MOCK_MODE=true`; `MOCK_MODE=1` код НЕ распознаёт (ловушка из .env.example). Запланирован фикс: принимать `1/true/yes` и поправить .env.example.
- Скрипты `tools/*.py` мок-режим не поддерживают вовсе — на машине с pigpiod они двигают реальное железо. На этом ноутбуке pigpio нет, но любые subprocess-пути (bridge/workflows) считать потенциально «боевыми».

## ИРБИС

Боевой контур не трогать. Тестового подключения нет (Роман, 2026-06-12).

Хост `172.29.67.70:6666` — боевой (подтверждено Романом); работоспособность подключения и кодировку (UTF-8 vs cp1251) проверить, когда будет доступ к RPi;
базы KAT%SERV09% (книги, индекс IN=), RDR (читатели); креды только из env `IRBIS_USERNAME`/`IRBIS_PASSWORD`.
Для разработки: `IRBIS_MOCK=true` (моки в `bookcabinet/irbis/mock.py`).

## БД

Перед миграциями — бэкап. Состояния полок и операций — источник правды
о физическом мире.

Живая БД — SQLite `bookcabinet/data/shelf_data.db` (бэкапы — `monitoring/backup.py`, `/api/backup/*`).
Alembic настроен, но схему создаёт `database/db.py`; не плодить расхождений между ними.

## Git

В main не коммитить. Ветки feature/* и fix/*, изменения через PR.
