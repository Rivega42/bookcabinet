# RRU9816 Sidecar Bridge

Этот C# sidecar обеспечивает связь между RRU9816 устройством и Node.js приложением через WebSocket.

## ✅ Рабочее состояние (September 2025)

**Успешно решенные проблемы:**
- ✅ **RRU9816 v03.01 полностью подключается** через COM2 @ 57600 baud
- ✅ **Обход неподдерживаемых команд** SetWorkMode и SetAntennaMultiplexing (возвращают код 254)
- ✅ **Dual-mode инвентаризация** - буферный режим с автофоллбэком на прямой режим
- ✅ **Обнаружение тегов работает** - система находит теги и отправляет их в веб-интерфейс
- ✅ **Исправлен парсинг EPC** - нет больше лишних нулей в конце тегов

## 🔧 Установка и сборка

### 1. Скопируйте RRU9816.dll
```bash
# Скопируйте RRU9816.dll в эту папку
copy path\to\RRU9816.dll .\RRU9816.dll
```

### 2. Сборка приложения
```bash
dotnet build --configuration Release
```

### 3. Запуск
```bash
dotnet run
```

## 📡 WebSocket API

Sidecar создает WebSocket сервер на `ws://localhost:8081/`

### Входящие команды:
```json
{
  "command": "connect",
  "port": "COM15",
  "baudRate": 57600
}

{
  "command": "disconnect"
}

{
  "command": "start_inventory"
}

{
  "command": "stop_inventory"
}
```

### Исходящие события:
```json
{
  "type": "connected",
  "port": "COM2",
  "baudRate": 57600,
  "message": "RRU9816 connected successfully via DLL"
}

{
  "type": "tag_read",
  "epc": "B75F196000210015983E",
  "rssi": -35.7,
  "timestamp": "2025-09-25T11:18:45.123Z",
  "readerType": "RRU9816-DIRECT"
}

{
  "type": "inventory_started",
  "message": "RFID Tag inventory started - RRU9816 is now scanning for tags!"
}

{
  "type": "error",
  "message": "Connection failed: Invalid port"
}
```

## 🔍 Техническая информация

### Режимы инвентаризации:
1. **Буферный режим** (InventoryBuffer_G2) - первоначальная попытка
2. **Прямой режим** (Inventory_G2) - fallback при 0 тегах в буфере
3. **Автоматическое переключение** - если буфер пустой, система переходит на прямой опрос каждые 200мс

### Обработка неподдерживаемых команд:
```
⚠️ Skipping SetWorkMode and SetAntennaMultiplexing (not supported by this firmware version)
```
RRU9816 v03.01 не поддерживает эти команды, система их пропускает и использует InAnt=0x01.

### Ожидаемые логи при успешном подключении:
```
✅ Connected to RRU9816 on COM2
✅ RRU9816 Info: Version 03.01, Power 26
✅ Address set
✅ Power set  
✅ Region set to EU band
🎯 RRU9816 initialization completed!
⚡ Buffer mode found no tags - switching to DIRECT inventory mode!
🏷️ DIRECT TAG: B75F196000210015983E (PC: 3000, Len: 12)
```

## 🎯 Использование

1. Запустите sidecar: `dotnet run`
2. Sidecar создаст WebSocket сервер на порту 8081
3. Node.js приложение подключится автоматически
4. Все команды RRU9816 будут обрабатываться через DLL

## 📋 Зависимости

- .NET 6.0
- RRU9816.dll (из демки)
- Newtonsoft.Json